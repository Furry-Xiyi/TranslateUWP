<Page
    x:Class="TranslatorApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <Grid x:Name="RootGrid" UseLayoutRounding="True">
        <Grid.Resources>
            <DataTemplate x:Key="WelcomeApiExpanderTemplate">
                <controls:Expander IsExpanded="False" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" MinHeight="80">
                    <controls:Expander.Header>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE946;" FontSize="20" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="如何获取 API 密钥" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="点击链接前往相关网站注册" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                        </Grid>
                    </controls:Expander.Header>
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                        <HyperlinkButton Content="Bing 翻译 API" NavigateUri="https://www.microsoft.com/en-us/translator/business/translator-api/"/>
                        <HyperlinkButton Content="百度翻译 API" NavigateUri="https://api.fanyi.baidu.com/"/>
                        <HyperlinkButton Content="有道翻译 API" NavigateUri="https://ai.youdao.com/"/>
                    </StackPanel>
                </controls:Expander>
            </DataTemplate>
        </Grid.Resources>

        <Grid x:Name="CustomDragRegion" UseLayoutRounding="True" Height="48" VerticalAlignment="Top" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 底层：扩展的拖拽区，跨三列，放在最底层并作为 SetTitleBar 的 target -->
            <Border x:Name="DragRegion"
            Grid.Column="0" Grid.ColumnSpan="3"
            Background="Transparent"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            IsHitTestVisible="True" />

            <!-- 左侧视觉（图标 + 文本）放在 DragRegion 之上 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8" Margin="12,0,0,0" Canvas.ZIndex="1">
                <Image x:Name="AppIconImage" Width="20" Height="20" Stretch="Uniform" IsHitTestVisible="False"/>
                <TextBlock x:Name="AppTitleText" Text="翻译" FontSize="16" VerticalAlignment="Center" IsHitTestVisible="False"/>
            </StackPanel>

            <!-- 中间宿主（居中，交互控件放在这里） -->
            <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Canvas.ZIndex="1">
                <Grid x:Name="TitleBarCenterHost" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{x:Null}" MinWidth="520"/>
            </Grid>

            <!-- 右侧头像 直接放在 DragRegion 之上，不包在 Button 里，Panel.ZIndex 最高 -->
            <controls:PersonPicture x:Name="AccountPicture"
                        Grid.Column="2"
                        Width="32"
                        Height="32"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,12,0"
                        Canvas.ZIndex="2"
                        Tapped="AccountPicture_Tapped">
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout x:Name="AccountMenu" Placement="Bottom">
                        <MenuFlyoutItem x:Name="LoginMenuItem" Text="登录" Click="LoginMenuItem_Click" />
                        <MenuFlyoutItem x:Name="SwitchAccountMenuItem" Text="切换账号" Click="SwitchAccountMenuItem_Click" Visibility="Collapsed" />
                        <MenuFlyoutItem x:Name="LogoutMenuItem" Text="注销" Click="LogoutMenuItem_Click" Visibility="Collapsed" />
                    </MenuFlyout>
                </FlyoutBase.AttachedFlyout>
            </controls:PersonPicture>
        </Grid>

        <Grid Margin="0,48,0,0">
            <controls:NavigationView x:Name="NavView" PaneDisplayMode="Left" IsSettingsVisible="True" IsBackButtonVisible="Visible" BackRequested="NavView_BackRequested" SelectionChanged="NavView_SelectionChanged">
                <controls:NavigationView.MenuItems>
                    <controls:NavigationViewItem x:Name="Nav_Online" Content="查词" Icon="Find" Tag="WordLookupPage"/>
                    <controls:NavigationViewItem x:Name="Nav_Lookup" Content="互译" Icon="Globe" Tag="OnlineTranslatePage"/>
                    <controls:NavigationViewItem x:Name="Nav_Fav" Content="收藏" Icon="Favorite" Tag="FavoritesPage"/>
                </controls:NavigationView.MenuItems>
                <Frame x:Name="ContentFrame"/>
            </controls:NavigationView>
        </Grid>

        <Grid x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
            <ProgressRing IsActive="True" Width="60" Height="60" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- 悬浮 InfoBar（置顶宿主） -->
        <Grid HorizontalAlignment="Stretch"
      VerticalAlignment="Top"
      IsHitTestVisible="False"
      Canvas.ZIndex="9999"
      Margin="12,12,12,0">
            <controls:InfoBar
        x:Name="TopInfoBar"
        IsOpen="False"
        Severity="Informational"
        IsClosable="False"
        HorizontalAlignment="Center"
        MaxWidth="980"
        Padding="12"
        RenderTransformOrigin="0.5,0"
        Message="">
                <controls:InfoBar.RenderTransform>
                    <TranslateTransform Y="-80"/>
                </controls:InfoBar.RenderTransform>
            </controls:InfoBar>
        </Grid>
    </Grid>
</Page>